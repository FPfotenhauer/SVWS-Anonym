[database]
host = localhost
port = 3306
user = your_username
password = your_password

[anonymization]
# Optional: Specify schemas to anonymize (comma-separated)
# If empty, the tool will prompt for selection
schemas = 

# Optional: Backup before anonymization (recommended)
create_backup = true
